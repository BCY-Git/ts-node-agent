# 我们使用 DeepSeek API 的时候，它会启动思考模式，这个思考模式在返回的时候会带有一个字段。
## 也就是let reasoning_content = "";这个Message的Push就需要做一些修改

javascript
```   
//@ts-ignore - DeepSeek API 需要 reasoning_content
        this.messages.push({ 
            role: "assistant", 
            content: content,
            reasoning_content: reasoning_content || undefined,
            tool_calls: tool_calls.map(call => ({ id: call.id, type: "function", function: { name: call.function.name, arguments: call.function.arguments } })) 
        } as any);
        return { content, tool_calls };   
```

## 在读取新闻的时候，如果让它读取的条数过多，有可能会把 Token 撑爆,可以少读几条进行测试